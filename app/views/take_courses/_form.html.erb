<%= form_for(@take_course) do |f| %>
  <% if @take_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@take_course.errors.count, "error") %> prohibited this take_course from being saved:</h2>

      <ul>
      <% @take_course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.number_field :course_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br>
    <%= f.number_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
