<!DOCTYPE html>

<h1><div class="cart_title">Courses you are taking</div></h1>

<table>
	<tr>
	
		<%= @cart.line_course.all %>
  		<%= render @cart.line_course.where(cart_id: '1') %>
  		
  </tr>
  
</table>

<%= button_to 'Empty cart', @cart, :method => :delete,
    :confirm => 'Are you sure?' %>
<%= button_to "Back", courses_path, :id => "open-contacts-dialog-btn", 
    :class => "inbox-sf-add-btn tip", :method => :get %></td>
